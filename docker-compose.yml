version: '3.8'
services:
  kokoro-tts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kokoro-tts
    restart: always
    expose:
      - "6007"
    networks:
      - coolify
networks:
  coolify:
    external: true